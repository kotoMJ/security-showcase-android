<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	package="cz.koto.keystorecompat">

	<uses-sdk tools:overrideLibrary="cz.koto.keystorecompat_19, cz.koto.keystorecompat_21, cz.koto.keystorecompat_23" />

	<application android:allowBackup="true"
		android:supportsRtl="true">

		<provider
			android:name=".KeystoreCompatInitProvider"
			android:authorities="${applicationId}.keystorecompatinitprovider"
			android:exported="false"
			android:enabled="true" />

		<receiver
			android:name=".SecurityDeviceAdmin"
			android:label="${kitkatDeviceAdminLabel}"
			android:description="@string/kc_auth_desc"
			android:permission="android.permission.BIND_DEVICE_ADMIN">
			<meta-data
				android:name="android.app.device_admin"
				android:resource="@xml/device_admin" />
			<intent-filter>
				<action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
				<action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
			</intent-filter>
		</receiver>

	</application>

</manifest>
